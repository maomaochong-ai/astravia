1.数据库设置面板的主要用于全局通用型配置，因为在数据库标签页面已经有了数据库工作台壳，二者职责要清晰划分。重新考虑设置面用什么内容来承载数据库相关的配置。

2.活动面板tab标签页面交互正常，但是标签页下方的sql查询面板中间多了j9bi→脏文本，另外让 AI 协助配置功能职责定位不清晰，在不同的地方应该具备不同的能力展示。比如在数据库工作台中可以进行问数并进行跳转。

3.活动面板tab标签页面的数据库工作壳目前三栏布局，尤其是连接详情设计没有很好的区分标题和数据库信息。比如主机和47.106.121.38:9286等。

4.活动面板数据库tab标签页面的数据库工作壳目前三栏布局目前比较简陋，功能设计上还达不到E:\open-source-projects\dbx-main的数据库工作壳水准。采用渐进式逼近的方式一步步实现。

5.设置面板和活动面板的数据库tab标签面板的设计如果都需要共同展示，那么需要认真评估怎么展示合适

6.视觉与 AI 双向联动中面板中让 AI 分析此表」→ 携带 schema 跳转对话后。自动发送的【让 AI 分析表「ods_pld_asset_pro_t_asset_project_info」（连接pgsql）的结构与数据质量。】继对话开场白不太适合，需要重新考虑。

7.如果将数据库tab页面的让 AI 分析此表后，我发送写出查询sql
并执行sql语句，没有在数据库标签页的工作台的sql查询面板中自动执行并查询.关于数据库工作台与现有的AI对话面板双向联动的设计要重点研发。

8.活动面板数据库tab标签页面的触发机制（多入口联动）要考虑全面。比如活动面板「+」菜单功能，设置页快捷，AI 对话联动，页面按钮等。

9.配色与当前项目不同视觉主题对齐，禁止另起一套色板。

10.其他要求
尽量UI风格保持一致性，不要对类似组件重复声明样式。
严禁卡片左侧有竖杠，充满ai味道。
减少卡片线条感，缺少质感
尽可能保证组件复用。例如同一个按钮弹窗不同页面不同大小，或者弹窗显示不完整。
严禁颜色搭配花里胡哨
尽量文字和卡片层级分明

10.修改后的代码要以架构师和代码质量工程师的角色进行代码审查。比如避免代码臃肿.

以上请你基于现有蓝图确定以上的改进措施并更新引用的文档，然后再告诉我当前已完成的进度和下一步任务。